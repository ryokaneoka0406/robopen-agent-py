# AGENTS.md

## このファイルの目的
この `AGENTS.md` は**リポジトリ（プロジェクト）そのもの**をAIエージェントが安全かつ一貫して開発するための運用ガイドである。
実行対象となる「個人用パーソナルエージェント」の人格定義ファイルではない点に注意する。

## プロジェクト概要（開発対象）
- 開発対象は、Codex CLIをコアにしたSlack連携の個人用パーソナルエージェント。
- 主な構成は Wrapper / Scheduler / SQLite Memory / Codex CLI 連携。
- 本リポジトリでは、上記システムの設計・実装・運用ドキュメントを段階的に整備する。

## 開発時の基本方針
- 設計ドキュメント（`documents/designdoc.md`）とタスク分解（`tasks/`）を常に同期する。
- 変更は小さく分割し、タスク単位でコミットする。
- 破壊的変更（大量削除・互換性破壊）は、理由・影響範囲・ロールバック方法を明記する。

## ドキュメント運用ルール
- `documents/`: 要件・設計・運用方針の一次情報を置く。
- `tasks/`: 命名規則は `NN_タスク概要.md` を維持する。
- `tasks/strategy.md`: 並行実行可能性と依存関係を管理する。

## 実装ルール
- 実装言語は Python を標準とし、パッケージ管理と仮想環境は `uv` を使用する。
- Slack連携・Scheduler・Memoryの責務を分離し、密結合を避ける。
- 永続化スキーマ変更時は、マイグレーション方針を同時に記載する。
- セキュリティ関連（トークン、権限、監査ログ）は機能追加より優先して担保する。

## セキュリティ/運用ルール
- シークレット情報（Token/鍵/認証情報）をリポジトリへコミットしない。
- `.env` や運用設定の取り扱い手順をドキュメント化し、再現可能性を保つ。
- 監査対象の操作（削除系、外部API破壊的操作）は仕様として明文化し、テスト観点を残す。

## 参照先
- 設計書: `documents/designdoc.md`
- タスク一覧: `tasks/*.md`
- 並行実行戦略: `tasks/strategy.md`

